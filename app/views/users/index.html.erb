<ul class="user-main">
  <% @users.each do |user| %>
    <%= link_to user_path(user) do%>
    <li>
        <%= image_tag user.photo %>
        <div>
        <h1><%= user.name %></h1>
        <p>Number of posts : <%= user.posts.size %></p>
        </div>
    </li>
    <% end %>
  <% end %>
    
</ul>

